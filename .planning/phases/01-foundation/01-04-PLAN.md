---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - backend/app/services/document_processor.py
  - backend/app/core/document_loaders.py
  - backend/app/api/routes/processing.py
  - backend/requirements.txt
autonomous: true
requirements: ["DOC-05", "DOC-06", "DOC-07", "DOC-08"]
must_haves:
  truths:
    - "System can extract text from PDF, DOCX, MD, TXT files"
    - "Documents are split into chunks of ~500 chars with 50 char overlap"
    - "Each chunk retains source document reference and chunk index"
    - "Processing status is tracked and retrievable"
  artifacts:
    - path: "backend/app/core/document_loaders.py"
      provides: "File parsing for different formats"
      min_lines: 60
    - path: "backend/app/services/document_processor.py"
      provides: "Text extraction and chunking orchestration"
      min_lines: 80
    - path: "backend/app/api/routes/processing.py"
      provides: "Processing trigger and status endpoints"
      min_lines: 40
  key_links:
    - from: "backend/app/core/document_loaders.py"
      to: "backend/app/services/document_processor.py"
      via: "Import and call parse functions"
    - from: "backend/app/services/document_processor.py"
      to: "LangChain TextSplitter"
      via: "RecursiveCharacterTextSplitter"
---

<objective>
Implement document parsing and chunking pipeline.

**Purpose:** Extract text from uploaded documents and split into manageable chunks for embedding and retrieval.
**Output:** Document processing service that handles multiple formats and produces optimized text chunks.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@backend/app/models/document.py
</context>

<tasks>

<task type="auto">
  <name>Install Document Processing Dependencies</name>
  <files>backend/requirements.txt</files>
  <action>
    Add document processing libraries to requirements.txt:
    - `PyPDF2>=3.0.1` - PDF text extraction
    - `python-docx>=1.1.0` - Word document parsing
    - `python-markdown>=3.5.1` - Markdown processing
    
    Ensure all dependencies are compatible with Python 3.11+.
    Do not install yet - just update requirements.txt.
  </action>
  <verify>
    <automated>grep -E "(PyPDF2|python-docx|python-markdown)" backend/requirements.txt</automated>
  </verify>
  <done>All document processing libraries listed in requirements.txt</done>
</task>

<task type="auto">
  <name>Create Document Loaders Module</name>
  <files>backend/app/core/document_loaders.py</files>
  <action>
    Create document loaders for different file formats:
    1. `load_pdf(file_path: str) -> str` - Extract text from PDF using PyPDF2
    2. `load_docx(file_path: str) -> str` - Extract text from Word documents
    3. `load_markdown(file_path: str) -> str` - Read and return markdown content
    4. `load_txt(file_path: str) -> str` - Read plain text files
    5. `get_loader(file_extension: str)` - Return appropriate loader function
    
    Requirements:
    - Handle encoding errors gracefully (try utf-8, fallback to latin-1)
    - Log warnings for files that fail to parse
    - Return empty string for unsupported formats
    - Strip excessive whitespace from extracted text
    - Handle multi-page PDFs (concatenate all pages)
  </action>
  <verify>
    <automated>
      cd backend && python -c "
from app.core.document_loaders import load_txt, get_loader
import tempfile, os

# Test basic loader
with tempfile.NamedTemporaryFile(mode='w', suffix='.txt', delete=False) as f:
    f.write('Hello World')
    temp_path = f.name

result = load_txt(temp_path)
os.unlink(temp_path)
assert result == 'Hello World', f'Expected Hello World, got {result}'
print('Document loaders working')
" 2>/dev/null || echo "Run: cd backend && python -m app.core.document_loaders"
    </automated>
  </verify>
  <done>All loader functions work correctly for their formats</done>
</task>

<task type="auto">
  <name>Create Document Processor Service</name>
  <files>backend/app/services/document_processor.py</files>
  <action>
    Create document processing service with:
    1. `process_document(document_id: str, file_path: str)` - Main entry point
       - Detect file type from extension
       - Load content using appropriate loader
       - Split into chunks using RecursiveCharacterTextSplitter
       - Return list of DocumentChunk objects
    2. `create_chunks(text: str, doc_id: str, chunk_size=500, chunk_overlap=50)` - Chunking logic
       - Use LangChain's RecursiveCharacterTextSplitter
       - Include metadata: source_doc_id, chunk_index, total_chunks
    3. `get_processing_status(document_id: str)` - Return processing state
    
    Import from:
    - `langchain.text_splitter import RecursiveCharacterTextSplitter`
    - `app.core.document_loaders` for format-specific loaders
    - `app.models.document` for chunk models
    
    Chunking strategy:
    - chunk_size: 500 characters
    - chunk_overlap: 50 characters  
    - separators: ["\n\n", "\n", ". ", " ", ""]
  </action>
  <verify>
    <automated>
      cd backend && python -c "
from app.services.document_processor import DocumentProcessor
from langchain.text_splitter import RecursiveCharacterTextSplitter

# Test chunking
processor = DocumentProcessor()
text = 'This is sentence one. ' * 100  # ~2000 chars
chunks = processor.create_chunks(text, 'test-doc', chunk_size=500, chunk_overlap=50)
assert len(chunks) > 0, 'No chunks created'
assert all(hasattr(c, 'page_content') for c in chunks), 'Chunks missing content'
print(f'Created {len(chunks)} chunks from {len(text)} chars')
print('Document processor working')
" 2>/dev/null || echo "Manual verification needed"
    </automated>
  </verify>
  <done>Document processor creates chunks with correct size/overlap and metadata</done>
</task>

<task type="auto">
  <name>Create Processing API Endpoints</name>
  <files>backend/app/api/routes/processing.py</files>
  <action>
    Create API endpoints for document processing:
    1. `POST /api/documents/{id}/process` - Trigger processing
       - Load document from database by ID
       - Call DocumentProcessor.process_document()
       - Return processing result with chunk count
    2. `GET /api/documents/{id}/status` - Get processing status
       - Return: pending, processing, completed, failed
       - Include chunk count if completed
       - Include error message if failed
    
    Integration:
    - Import DocumentService from 01-03
    - Import DocumentProcessor from this plan
    - Wire into main.py with /api/documents prefix
    
    Use response models from app.models.document
  </action>
  <verify>
    <automated>
      cd backend && python -c "
from app.main import app
from fastapi.testclient import TestClient
client = TestClient(app)

# Test endpoint exists
response = client.get('/api/documents/nonexistent/status')
print(f'Status endpoint: {response.status_code}')

# Should get 404 for non-existent doc
assert response.status_code in [404, 200], f'Unexpected status: {response.status_code}'
print('Processing endpoints accessible')
" 2>/dev/null || echo "Verify with: curl http://localhost:8000/api/documents"
    </automated>
  </verify>
  <done>Processing endpoints return correct status codes and data</done>
</task>

<task type="auto">
  <name>Integrate Processing into Upload Flow</name>
  <files>backend/app/api/routes/documents.py</files>
  <action>
    Modify the upload endpoint to optionally trigger processing:
    1. Add `process: bool = True` query parameter to POST /api/documents
    2. If process=True, trigger DocumentProcessor after saving file
    3. Return processing status in upload response
    4. Ensure processing happens asynchronously (use background tasks)
    
    Update imports:
    - `from fastapi import BackgroundTasks`
    - `from app.services.document_processor import DocumentProcessor`
    
    The upload response should include:
    - document info
    - processing_status (pending/processing/completed)
    - chunk_count (if completed)
  </action>
  <verify>
    <automated>
      cd backend && python -c "
# Verify background tasks import and usage
with open('app/api/routes/documents.py', 'r') as f:
    content = f.read()
    assert 'BackgroundTasks' in content, 'Missing BackgroundTasks import'
    assert 'process' in content.lower(), 'Missing process parameter'
    print('Upload endpoint integrated with processing')
"
    </automated>
  </verify>
  <done>Upload endpoint accepts process parameter and triggers background processing</done>
</task>

</tasks>

<verification>
1. **Upload and process:** POST a PDF file with process=true, verify chunks created
2. **Check status:** GET /api/documents/{id}/status shows completed with chunk count
3. **Test formats:** Verify PDF, DOCX, MD, TXT all parse correctly
4. **Check chunking:** Verify chunks are ~500 chars with 50 char overlap
5. **Error handling:** Try uploading corrupted file, verify graceful failure
</verification>

<success_criteria>
- All four document formats (PDF, DOCX, MD, TXT) parse correctly
- Text extracted and cleaned (extra whitespace removed)
- Chunks created with 500 char size and 50 char overlap
- Each chunk has metadata: source_doc_id, chunk_index, total_chunks
- Processing status tracked (pending/processing/completed/failed)
- Processing runs as background task (non-blocking upload)
- Failed processing is logged and status reflects error
- API endpoints for status checking work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
