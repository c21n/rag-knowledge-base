---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - backend/app/api/routes/documents.py
  - backend/app/api/routes/__init__.py
  - backend/app/models/document.py
  - backend/app/services/document_service.py
  - backend/app/core/config.py
  - backend/app/main.py
autonomous: true
requirements: ["DOC-01", "DOC-02", "DOC-03", "DOC-04", "API-03", "API-04", "API-05"]
must_haves:
  truths:
    - "API accepts PDF, DOCX, MD, TXT file uploads via POST /api/documents"
    - "Files are saved to local storage with metadata"
    - "API returns list of uploaded documents with metadata"
    - "Documents can be deleted via DELETE endpoint"
  artifacts:
    - path: "backend/app/api/routes/documents.py"
      provides: "REST endpoints for document CRUD operations"
      min_lines: 80
    - path: "backend/app/services/document_service.py"
      provides: "Business logic for document management"
      min_lines: 50
    - path: "backend/app/models/document.py"
      provides: "Pydantic models for document data"
      exports: ["DocumentCreate", "DocumentResponse", "DocumentList"]
  key_links:
    - from: "backend/app/api/routes/documents.py"
      to: "backend/app/services/document_service.py"
      via: "Dependency injection"
    - from: "backend/app/services/document_service.py"
      to: "uploads/ directory"
      via: "File I/O operations"
---

<objective>
Create REST API endpoints for document upload, listing, and deletion.

**Purpose:** Enable users to upload and manage documents that will be processed by the RAG system.
**Output:** Fully functional document management API with file storage.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Create Document Models</name>
  <files>backend/app/models/document.py</files>
  <action>
    Create Pydantic models for document operations:
    1. `DocumentBase` with filename, content_type, size
    2. `DocumentCreate` for upload requests
    3. `DocumentResponse` for API responses with id, filename, content_type, size, created_at
    4. `DocumentList` for listing endpoint (items + total count)
    5. Add file size limit validation (50MB max)
    
    Use Pydantic v2 syntax. Include example values in Field descriptions.
  </action>
  <verify>
    <automated>python -c "from backend.app.models.document import DocumentResponse; print('Models OK')"</automated>
  </verify>
  <done>All document models import successfully and validate correctly</done>
</task>

<task type="auto">
  <name>Create Document Service</name>
  <files>backend/app/services/document_service.py</files>
  <action>
    Create document management service with:
    1. `save_document(file, filename)` - Save uploaded file to uploads directory, return file info
    2. `get_document(id)` - Retrieve document metadata by ID
    3. `list_documents()` - List all documents with metadata from database
    4. `delete_document(id)` - Remove file from disk and database record
    
    Service should:
    - Generate unique IDs for files (use UUID)
    - Store files in `uploads/` directory (create if not exists)
    - Track metadata in SQLite via SQLAlchemy models from 01-02
    - Handle file not found errors gracefully
    - Log all operations
  </action>
  <verify>
    <automated>python -c "from backend.app.services.document_service import DocumentService; print('Service OK')"</automated>
  </verify>
  <done>Document service initializes and all methods callable</done>
</task>

<task type="auto">
  <name>Create Document API Routes</name>
  <files>backend/app/api/routes/documents.py</files>
  <action>
    Create FastAPI routes for document management:
    1. `POST /api/documents` - Upload document
       - Accept multipart/form-data with file field
       - Validate file type (pdf, docx, md, txt)
       - Validate file size (50MB max)
       - Return DocumentResponse
    2. `GET /api/documents` - List all documents
       - Return DocumentList with all uploaded documents
    3. `DELETE /api/documents/{id}` - Delete document
       - Return 204 on success
       - Return 404 if not found
    
    Include proper:
    - HTTP status codes
    - Error responses (400 for invalid, 404 for not found)
    - Response models
    - File type validation using UploadFile
    - Dependency injection for DocumentService
  </action>
  <verify>
    <automated>
      cd backend && python -c "
import requests
from app.main import app
from fastapi.testclient import TestClient
client = TestClient(app)

# Test endpoints exist
print('Testing /api/documents endpoint...')
response = client.get('/api/documents')
print(f'GET /api/documents: {response.status_code}')

# Test upload (create dummy file)
from io import BytesIO
files = {'file': ('test.txt', BytesIO(b'Hello World'), 'text/plain')}
response = client.post('/api/documents', files=files)
print(f'POST /api/documents: {response.status_code}')
print('API routes working')
" 2>/dev/null || echo "Run manual test with: cd backend && uvicorn app.main:app --reload"
    </automated>
  </verify>
  <done>All three endpoints respond correctly; upload saves file to disk</done>
</task>

<task type="auto">
  <name>Wire Routes to Main App</name>
  <files>backend/app/main.py</files>
  <action>
    Integrate document routes into main FastAPI application:
    1. Import documents router from app.api.routes.documents
    2. Include router with prefix="/api/documents"
    3. Add tags=["documents"] for API docs
    4. Ensure uploads directory is created on startup if not exists
    5. Add startup event handler to log available routes
    
    Verify the route is accessible at /api/documents and shows in /docs.
  </action>
  <verify>
    <automated>
      cd backend && python -c "
from app.main import app
routes = [r.path for r in app.routes if hasattr(r, 'path')]
assert '/api/documents' in routes, 'Documents route not found'
assert '/api/health' in routes, 'Health route missing'
print('All routes registered:')
for r in sorted(routes):
    if r.startswith('/api'):
        print(f'  {r}')
" 2>/dev/null || echo "Check routes manually with FastAPI docs"
    </automated>
  </verify>
  <done>/api/documents accessible, /docs shows document endpoints</done>
</task>

</tasks>

<verification>
1. **Test file upload:** Use curl or API client to POST a PDF/text file to /api/documents
2. **Test listing:** GET /api/documents returns list with uploaded file
3. **Test deletion:** DELETE /api/documents/{id} removes file and record
4. **Check docs:** Swagger UI at /docs shows all document endpoints
5. **Verify storage:** Uploaded files exist in backend/uploads/ directory
</verification>

<success_criteria>
- POST /api/documents accepts and saves PDF, DOCX, MD, TXT files
- GET /api/documents returns list with metadata (filename, size, upload date)
- DELETE /api/documents/{id} removes file from disk and database
- File size validation rejects files >50MB
- File type validation rejects unsupported formats
- All endpoints documented in Swagger UI
- Files stored in uploads/ directory with unique IDs
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
