---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - backend/src/models/__init__.py
  - backend/src/models/database.py
  - backend/src/models/document.py
  - backend/src/vectorstore/__init__.py
  - backend/src/vectorstore/chroma_client.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/alembic/versions/.gitkeep
autonomous: true
requirements:
  - VEC-02
  - VEC-03
  - DOC-07
  - DOC-08
must_haves:
  truths:
    - "SQLite database schema is defined and migratable"
    - "Document model exists with proper fields"
    - "ChromaDB client can connect and persist data"
    - "Vector collections can be created and queried"
    - "Database persists across application restarts"
  artifacts:
    - path: "backend/src/models/document.py"
      provides: "SQLAlchemy Document model"
      contains: ["class Document", "id", "filename", "uploaded_at"]
    - path: "backend/src/vectorstore/chroma_client.py"
      provides: "ChromaDB client wrapper"
      exports: ["ChromaClient", "get_chroma_client"]
    - path: "backend/src/models/database.py"
      provides: "Database connection and session management"
      exports: ["Base", "engine", "SessionLocal", "get_db"]
  key_links:
    - from: "backend/src/models/document.py"
      to: "backend/src/models/database.py"
      via: "Base class inheritance"
      pattern: "from.*database.*import.*Base"
    - from: "backend/src/vectorstore/chroma_client.py"
      to: "backend/src/config.py"
      via: "Settings import for CHROMA_PERSIST_DIR"
      pattern: "from.*config.*import"
user_setup: []
---

<objective>
Set up the database layer with SQLAlchemy models for document metadata and create the ChromaDB vector store client for embedding storage. Both storage systems must persist data across restarts.

Purpose: Provide data persistence foundations required for document upload, listing, and retrieval operations in subsequent plans.
Output: Working database models and vector store client ready for document processing pipeline integration.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQLAlchemy database infrastructure</name>
  <files>
    backend/src/models/__init__.py
    backend/src/models/database.py
    backend/alembic.ini
    backend/alembic/env.py
    backend/alembic/script.py.mako
    backend/alembic/versions/.gitkeep
  </files>
  <action>
    Create SQLAlchemy database setup:

    1. Create backend/src/models/__init__.py exposing Base, engine, SessionLocal, get_db

    2. Create backend/src/models/database.py with:
       - Import create_engine, sessionmaker, declarative_base from sqlalchemy
       - Import Settings from config module
       - Create async-compatible engine (can use sync for simplicity)
       - SessionLocal factory for database sessions
       - Base class for declarative models
       - get_db() dependency function for FastAPI
       - create_tables() function to initialize schema

    3. Set up Alembic for database migrations:
       - Create alembic.ini with SQLite configuration
       - Create alembic/ directory structure
       - Configure alembic/env.py to use our Base metadata
       - Include script.py.mako template

    Ensure all imports reference the correct modules from 01-01 plan.
  </action>
  <verify>
    <automated>cd backend && python -c "from src.models.database import Base, engine, SessionLocal, get_db; print('Database imports OK')"</automated>
    <automated>cd backend && python -c "from src.config import get_settings; print(f'DB URL: {get_settings().DATABASE_URL}')"</automated>
  </verify>
  <done>Database module created; Alembic configured; imports work correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create Document SQLAlchemy model</name>
  <files>
    backend/src/models/document.py
  </files>
  <action>
    Create the Document model for tracking uploaded documents:

    1. Create backend/src/models/document.py with Document class inheriting from Base

    2. Document model fields:
       - id: UUID primary key (use UUID type or String)
       - filename: String (original filename)
       - file_path: String (path where file is stored)
       - file_type: String (pdf, docx, md, txt)
       - file_size: Integer (size in bytes)
       - chunk_count: Integer (number of chunks created, default 0)
       - status: String (uploaded, processing, completed, failed)
       - error_message: String (nullable, for failure details)
       - uploaded_at: DateTime (auto-set on creation)
       - processed_at: DateTime (nullable, when processing completed)

    3. Include:
       - __tablename__ = "documents"
       - __repr__ method for debugging
       - to_dict() method for JSON serialization

    4. Update backend/src/models/__init__.py to export Document

    Reference requirements DOC-07 and DOC-08 which need document listing/deletion.
  </action>
  <verify>
    <automated>cd backend && python -c "from src.models.document import Document; print('Document model imports OK')"</automated>
    <automated>cd backend && python -c "from src.models.database import Base; from src.models.document import Document; print('Base and Document compatible')"</automated>
  </verify>
  <done>Document model created with all required fields; exports correctly</done>
</task>

<task type="auto">
  <name>Task 3: Create ChromaDB vector store client</name>
  <files>
    backend/src/vectorstore/__init__.py
    backend/src/vectorstore/chroma_client.py
  </files>
  <action>
    Create the ChromaDB client wrapper:

    1. Create backend/src/vectorstore/__init__.py exposing ChromaClient, get_chroma_client

    2. Create backend/src/vectorstore/chroma_client.py with:
       - Import chromadb and chromadb.config
       - Import Settings from config module
       - ChromaClient class:
         * __init__(persist_dir: str, embedding_function=None)
         * _get_client() -> chromadb.Client - internal method to get/create client
         * get_or_create_collection(name: str) -> Collection
         * delete_collection(name: str) -> bool
         * list_collections() -> List[str]
         * add_documents(collection_name, documents, embeddings, ids, metadatas)
         * query(collection_name, query_embedding, n_results=5)
       - get_chroma_client() function with caching (similar to get_settings)
       - Proper error handling for connection issues

    3. The client should:
       - Use PersistentClient for data persistence (VEC-03)
       - Support configurable persist directory from Settings
       - Handle collection creation and management
       - Provide clean interface for add/query operations

    4. Create a simple test to verify ChromaDB can initialize and persist
  </action>
  <verify>
    <automated>cd backend && python -c "from src.vectorstore.chroma_client import ChromaClient, get_chroma_client; print('Chroma client imports OK')"</automated>
    <automated>cd backend && python -c "from src.vectorstore import get_chroma_client; client = get_chroma_client(); cols = client.list_collections(); print(f'ChromaDB connected, collections: {cols}')"</automated>
  </verify>
  <done>ChromaDB client created; can connect and list collections; data persists to disk</done>
</task>

<task type="auto">
  <name>Task 4: Initialize database tables and verify persistence</name>
  <files>
    backend/src/models/__init__.py
    backend/alembic/versions/001_initial.py
  </files>
  <action>
    Initialize the database schema:

    1. Create initial Alembic migration:
       - Generate migration file in alembic/versions/001_initial.py
       - Include Document table creation
       - Include any indexes needed

    2. Create initialization script to:
       - Create database file if not exists
       - Run migrations or create_tables()
       - Verify tables are created

    3. Test the complete stack:
       - Import all models
       - Create a test document record
       - Verify it's persisted to SQLite
       - Verify ChromaDB creates its files in data/chroma/

    4. Update backend/src/models/__init__.py if needed
  </action>
  <verify>
    <automated>cd backend && python -c "from src.models.database import engine, Base; from src.models.document import Document; Base.metadata.create_all(bind=engine); print('Tables created')"</automated>
    <automated>cd backend && python -c "import os; db_exists = os.path.exists('data/app.db'); print(f'Database file exists: {db_exists}')"</automated>
    <automated>cd backend && python -c "import os; chroma_exists = os.path.exists('data/chroma'); print(f'Chroma directory exists: {chroma_exists}')"</automated>
  </verify>
  <done>Database tables initialized; SQLite file created; ChromaDB persistence verified</done>
</task>

</tasks>

<verification>
Overall Phase 1 Plan 2 verification:
1. Database models can be imported without errors
2. Document model has all required fields (id, filename, file_path, file_type, etc.)
3. ChromaDB client connects and creates collections
4. Data persists after application restart (check data/ directory has files)
5. Alembic migration can be applied
</verification>

<success_criteria>
- SQLAlchemy models defined and importable
- Document model with all metadata fields (id, filename, file_path, file_type, file_size, chunk_count, status, uploaded_at, processed_at)
- ChromaDB client wrapper with get_or_create_collection, add_documents, query methods
- Database persists to SQLite file in data/app.db
- Vector data persists to data/chroma/ directory
- Alembic migrations configured and runnable
- All imports work without circular dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
