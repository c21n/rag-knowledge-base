---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - backend/app/services/embedding_service.py
  - backend/app/core/vector_store.py
  - backend/app/models/chunk.py
  - backend/requirements.txt
  - backend/.env.example
autonomous: false
requirements: ["VEC-01", "VEC-02", "VEC-03", "VEC-04", "API-06", "API-07"]
must_haves:
  truths:
    - "Text chunks are converted to embeddings using OpenAI API"
    - "Embeddings stored in ChromaDB with metadata for retrieval"
    - "Vector data persists across application restarts"
    - "Similarity search returns top-5 most relevant chunks"
  artifacts:
    - path: "backend/app/services/embedding_service.py"
      provides: "OpenAI embedding generation"
      min_lines: 60
    - path: "backend/app/core/vector_store.py"
      provides: "ChromaDB integration and search"
      min_lines: 80
    - path: "backend/app/models/chunk.py"
      provides: "Chunk data models"
      exports: ["Chunk", "ChunkWithEmbedding", "SearchResult"]
  key_links:
    - from: "backend/app/services/embedding_service.py"
      to: "OpenAI API"
      via: "langchain-openai OpenAIEmbeddings"
    - from: "backend/app/core/vector_store.py"
      to: "ChromaDB"
      via: "chromadb.Client with persistent storage"
    - from: "backend/app/services/document_processor.py"
      to: "backend/app/core/vector_store.py"
      via: "Store chunks after processing"
user_setup:
  - service: OpenAI
    why: "Embedding generation requires OpenAI API key"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API Keys"
    instructions: "Create .env file with OPENAI_API_KEY before running"
---

<objective>
Implement embedding generation and vector storage pipeline.

**Purpose:** Convert text chunks into vector embeddings for semantic search and retrieval.
**Output:** Working embedding service integrated with ChromaDB for persistent vector storage.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
@backend/app/services/document_processor.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <decision>OpenAI API Key Setup</decision>
  <context>
    Embedding generation requires an OpenAI API key. This step creates the .env file template
    and verifies the API key is available before proceeding.
  </context>
  <instructions>
    1. Get your OpenAI API key from https://platform.openai.com/api-keys
    2. Create backend/.env file with: OPENAI_API_KEY=sk-your-key-here
    3. Run: echo "OPENAI_API_KEY=sk-..." > backend/.env
    
    The embedding service will fail without a valid API key.
  </instructions>
  <resume-signal>Type "ready" after creating backend/.env with OPENAI_API_KEY</resume-signal>
</task>

<task type="auto">
  <name>Install Embedding Dependencies</name>
  <files>backend/requirements.txt</files>
  <action>
    Add embedding and vector store libraries to requirements.txt:
    - `langchain-openai>=0.0.5` - OpenAI integration for LangChain
    - `chromadb>=0.4.15` - Vector database
    - `openai>=1.0.0` - OpenAI SDK (peer dependency)
    - `tiktoken>=0.5.0` - Token counting for OpenAI
    
    Also update .env.example with OPENAI_API_KEY placeholder.
  </action>
  <verify>
    <automated>grep -E "(langchain-openai|chromadb)" backend/requirements.txt</automated>
  </verify>
  <done>All embedding dependencies listed in requirements.txt</done>
</task>

<task type="auto">
  <name>Create Chunk Models</name>
  <files>backend/app/models/chunk.py</files>
  <action>
    Create Pydantic models for chunks and embeddings:
    1. `ChunkBase` - content, document_id, chunk_index, metadata
    2. `Chunk` - includes id and created_at
    3. `ChunkWithEmbedding` - Chunk + embedding vector
    4. `SearchResult` - chunk + similarity score
    5. `SearchResponse` - list of results + query info
    
    Models should include:
    - Validation that embedding is list of floats
    - Document ID reference for source tracking
    - Chunk index for ordering
    - Config for ORM mode if needed
  </action>
  <verify>
    <automated>
      cd backend && python -c "
from app.models.chunk import Chunk, SearchResult
chunk = Chunk(id='test', content='Hello', document_id='doc1', chunk_index=0)
print(f'Chunk model OK: {chunk.id}')
result = SearchResult(chunk=chunk, score=0.95)
print(f'SearchResult model OK: score={result.score}')
" 2>/dev/null || echo "Run: cd backend && python -m app.models.chunk"
    </automated>
  </verify>
  <done>Chunk models validate correctly and have all required fields</done>
</task>

<task type="auto">
  <name>Create Embedding Service</name>
  <files>backend/app/services/embedding_service.py</files>
  <action>
    Create embedding generation service:
    1. `EmbeddingService` class with:
       - `__init__()` - Initialize OpenAIEmbeddings with API key from settings
       - `generate_embedding(text: str) -> List[float]` - Single text embedding
       - `generate_embeddings(texts: List[str]) -> List[List[float]]` - Batch embedding
       - `get_embedding_model()` - Return embedding model name
    2. Use `langchain_openai.embeddings.OpenAIEmbeddings`
    3. Model: `text-embedding-ada-002`
    4. Add error handling for API failures
    5. Add rate limiting awareness (optional, log warnings)
    
    Configuration from environment:
    - OPENAI_API_KEY (required)
    - OPENAI_EMBEDDING_MODEL (optional, default: text-embedding-ada-002)
    
    Include logging for each embedding request (text length, timestamp).
  </action>
  <verify>
    <automated>
      cd backend && python -c "
import os
os.environ['OPENAI_API_KEY'] = 'test-key'
from app.services.embedding_service import EmbeddingService
service = EmbeddingService()
print(f'Model: {service.get_embedding_model()}')
# Test without actual API call
print('Embedding service initialized')
" 2>/dev/null || echo "Import test only - actual embedding requires API key"
    </automated>
  </verify>
  <done>Embedding service initializes with OpenAI, methods are callable</done>
</task>

<task type="auto">
  <name>Create Vector Store Module</name>
  <files>backend/app/core/vector_store.py</files>
  <action>
    Create ChromaDB integration for vector storage:
    1. `VectorStore` class with:
       - `__init__()` - Initialize ChromaDB client with persistent storage
       - `add_chunks(chunks: List[Chunk], embeddings: List[List[float]])` - Store chunks with embeddings
       - `search(query: str, top_k: int = 5) -> List[SearchResult]` - Semantic search
       - `delete_by_document(document_id: str)` - Remove all chunks for a document
       - `get_stats()` - Return collection stats (total chunks, documents)
    2. Storage configuration:
       - Use persistent ChromaDB (chromadb.PersistentClient)
       - Store path: `./data/chromadb`
       - Collection name: "documents"
    3. Metadata storage:
       - document_id: source document reference
       - chunk_index: position in original document
       - content: original text (for retrieval)
    4. Search functionality:
       - Convert query to embedding
       - Query ChromaDB with n_results=top_k
       - Return SearchResult objects with similarity scores
    
    Handle ChromaDB client initialization errors gracefully.
  </action>
  <verify>
    <automated>
      cd backend && python -c "
from app.core.vector_store import VectorStore
store = VectorStore()
print(f'VectorStore initialized')
stats = store.get_stats()
print(f'Stats: {stats}')
" 2>/dev/null || echo "ChromaDB requires directory creation first"
    </automated>
  </verify>
  <done>Vector store initializes and can add/search/delete chunks</done>
</task>

<task type="auto">
  <name>Integrate Embedding into Processing Pipeline</name>
  <files>backend/app/services/document_processor.py</files>
  <action>
    Update document processor to generate and store embeddings:
    1. Add imports:
       - `from app.services.embedding_service import EmbeddingService`
       - `from app.core.vector_store import VectorStore`
    2. Modify `process_document()`:
       - After chunking, extract chunk texts
       - Call embedding_service.generate_embeddings() for batch
       - Call vector_store.add_chunks() with chunks and embeddings
       - Update processing status to "completed" with chunk count
    3. Add error handling:
       - Catch embedding API errors
       - Set status to "failed" with error message
       - Log full error details
    4. Add method `reprocess_document()` for re-embedding if needed
    
    The processor should now:
    - Extract text → Chunk → Embed → Store in ChromaDB
    - Update status at each step
    - Handle failures gracefully
  </action>
  <verify>
    <automated>
      cd backend && python -c "
# Verify integration imports
import sys
sys.path.insert(0, '.')
with open('app/services/document_processor.py', 'r') as f:
    content = f.read()
    assert 'EmbeddingService' in content or 'embedding_service' in content, 'Missing embedding integration'
    assert 'VectorStore' in content or 'vector_store' in content, 'Missing vector store integration'
    print('Document processor integrated with embedding pipeline')
"
    </automated>
  </verify>
  <done>Document processor generates embeddings and stores in ChromaDB</done>
</task>

<task type="auto">
  <name>Create Search API Endpoint</name>
  <files>backend/app/api/routes/search.py</files>
  <action>
    Create semantic search endpoint:
    1. `POST /api/search` - Search document chunks
       - Request body: `{ "query": "string", "top_k": 5 }`
       - Response: SearchResponse with results
       - Each result includes: chunk content, document_id, score
    2. Use VectorStore.search() for retrieval
    3. Return formatted results with similarity scores
    4. Include validation for query (min 1 char, max 1000)
    
    Add route to main.py with prefix="/api".
    
    This endpoint will be used by the RAG engine in Phase 2.
  </action>
  <verify>
    <automated>
      cd backend && python -c "
from app.main import app
from fastapi.testclient import TestClient
client = TestClient(app)

# Test endpoint exists
response = client.post('/api/search', json={'query': 'test', 'top_k': 3})
print(f'Search endpoint: {response.status_code}')
# Should work even with empty DB
assert response.status_code in [200, 404], f'Unexpected status: {response.status_code}'
print('Search endpoint accessible')
" 2>/dev/null || echo "Verify with: curl -X POST http://localhost:8000/api/search -H 'Content-Type: application/json' -d '{\"query\":\"test\"}'"
    </automated>
  </verify>
  <done>Search endpoint accepts queries and returns results with scores</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete embedding pipeline integrated with ChromaDB</what-built>
  <how-to-verify>
    1. Start the server: `cd backend && uvicorn app.main:app --reload`
    2. Upload a document: `curl -X POST -F "file=@test.pdf" http://localhost:8000/api/documents?process=true`
    3. Check processing status: `curl http://localhost:8000/api/documents/{id}/status`
    4. Test search: `curl -X POST -H "Content-Type: application/json" -d '{"query":"your query","top_k":5}' http://localhost:8000/api/search`
    5. Verify ChromaDB persistence: `ls backend/data/chromadb/`
    6. Check API docs: Open http://localhost:8000/docs
    
    Expected results:
    - Upload returns document with processing_status
    - Status eventually shows "completed" with chunk count
    - Search returns relevant chunks with similarity scores (0-1)
    - ChromaDB files exist in data/chromadb/
  </how-to-verify>
  <resume-signal>Type "approved" when search returns relevant results from uploaded documents</resume-signal>
</task>

</tasks>

<verification>
1. **Upload and embed:** Upload PDF, verify chunks are embedded and stored
2. **Test search:** Submit query, verify top-5 results returned with scores
3. **Check persistence:** Restart server, verify data still searchable
4. **Verify API docs:** Check /docs shows search endpoint
5. **Test deletion:** Delete document, verify chunks removed from ChromaDB
6. **Error handling:** Test with invalid API key, verify graceful error
</verification>

<success_criteria>
- OpenAI embeddings generated for all document chunks
- ChromaDB persists data to disk (./data/chromadb/)
- POST /api/search returns top-5 most relevant chunks with similarity scores
- Each search result includes: content, document_id, chunk_index, score
- Embedding pipeline integrated into document processing flow
- Data survives application restart
- API documentation shows all endpoints at /docs
- Health check endpoint /api/health returns 200 OK
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
