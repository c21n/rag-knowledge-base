---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/.env.example
  - backend/.gitignore
  - backend/pyproject.toml
  - backend/src/main.py
  - backend/src/config.py
  - backend/src/__init__.py
autonomous: true
requirements:
  - API-07
must_haves:
  truths:
    - "Python virtual environment is configured"
    - "Backend dependencies are installable via pip"
    - "FastAPI server starts on port 8000"
    - "Auto-generated API docs available at /docs"
    - "Environment configuration is documented"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "Python dependencies list"
      min_lines: 10
    - path: "backend/src/main.py"
      provides: "FastAPI application entry point"
      exports: ["app"]
    - path: "backend/src/config.py"
      provides: "Configuration management"
      contains: ["Settings", "get_settings"]
  key_links:
    - from: "backend/src/main.py"
      to: "backend/src/config.py"
      via: "import Settings"
      pattern: "from.*config.*import"
user_setup:
  - service: OpenAI API
    why: "Required for embeddings and LLM in later phases"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API Keys"
    dashboard_config:
      - task: "Create API key with access to GPT-4 and embeddings"
        location: "https://platform.openai.com/api-keys"
---

<objective>
Set up the backend project structure with FastAPI, install all required dependencies, configure environment management, and verify the API server starts successfully with auto-generated documentation.

Purpose: Establish the foundation that all subsequent backend work depends on. Without this base setup, nothing else in Phase 1 can proceed.
Output: A working FastAPI application with dependency management and configuration handling ready for document pipeline development.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure and configuration files</name>
  <files>
    backend/requirements.txt
    backend/.env.example
    backend/.gitignore
    backend/pyproject.toml
  </files>
  <action>
    Create the backend project structure:

    1. Create backend/requirements.txt with all Phase 1 dependencies:
       - fastapi==0.104.1
       - uvicorn[standard]==0.24.0
       - pydantic==2.5.0
       - pydantic-settings==2.1.0
       - python-multipart==0.0.6
       - python-dotenv==1.0.0
       - langchain==0.1.0
       - langchain-openai==0.0.5
       - chromadb==0.4.15
       - pypdf2==3.0.1
       - python-docx==1.1.0
       - sqlalchemy==2.0.23
       - alembic==1.12.1
       - pytest==7.4.0
       - httpx==0.25.0

    2. Create backend/.env.example with all required environment variables:
       - OPENAI_API_KEY=your_openai_api_key_here
       - DATABASE_URL=sqlite:///./data/app.db
       - CHROMA_PERSIST_DIR=./data/chroma
       - UPLOAD_DIR=./data/uploads
       - EMBEDDING_MODEL=text-embedding-ada-002
       - CHUNK_SIZE=500
       - CHUNK_OVERLAP=50

    3. Create backend/.gitignore ignoring:
       - __pycache__/
       - *.pyc
       - .env
       - data/
       - .venv/
       - venv/
       - *.db

    4. Create backend/pyproject.toml with project metadata and black configuration.
  </action>
  <verify>
    <automated>cd backend && pip install -r requirements.txt --dry-run 2>&1 | head -20</automated>
    <manual>Verify requirements.txt has 15+ dependencies listed</manual>
  </verify>
  <done>All configuration files exist with correct content; dependencies list is complete</done>
</task>

<task type="auto">
  <name>Task 2: Create FastAPI application and configuration module</name>
  <files>
    backend/src/__init__.py
    backend/src/config.py
    backend/src/main.py
  </files>
  <action>
    Create the FastAPI application core:

    1. Create backend/src/__init__.py (empty init file for package)

    2. Create backend/src/config.py with:
       - Settings class using Pydantic Settings
       - All environment variables from .env.example
       - Type hints for all fields
       - Default values where appropriate
       - get_settings() function with lru_cache

    3. Create backend/src/main.py with:
       - FastAPI app instance with title and description
       - Root endpoint returning {"message": "RAG Knowledge Base API", "version": "1.0.0"}
       - Health check endpoint GET /api/health returning {"status": "healthy"}
       - API documentation auto-generated at /docs and /redoc
       - CORSMiddleware configured for frontend communication
       - Import and use Settings from config module

    Ensure all imports work correctly and no syntax errors.
  </action>
  <verify>
    <automated>cd backend && python -c "from src.main import app; from src.config import Settings; print('Imports OK')"</automated>
    <automated>cd backend && python -c "from src.config import get_settings; s = get_settings(); print(f'Chunk size: {s.CHUNK_SIZE}')"</automated>
  </verify>
  <done>FastAPI app and config modules created; imports work without errors; Settings can be instantiated</done>
</task>

<task type="auto">
  <name>Task 3: Create data directories and verify server startup</name>
  <files>
    backend/data/.gitkeep
    backend/data/uploads/.gitkeep
    backend/data/chroma/.gitkeep
  </files>
  <action>
    Set up data directories and verify the application:

    1. Create directory structure:
       - backend/data/
       - backend/data/uploads/
       - backend/data/chroma/

    2. Create .gitkeep files in each directory so they're tracked in git but contents are ignored

    3. Verify the FastAPI server can start:
       - Create a test script that imports the app
       - Verify no import errors
       - Test that uvicorn can load the application

    4. Document startup command in a README.md in backend/ directory with:
       - Installation instructions
       - Environment setup
       - How to run the server
       - How to access API docs
  </action>
  <verify>
    <automated>cd backend && python -c "import uvicorn; from src.main import app; print('Uvicorn can load app')"</automated>
    <automated>cd backend && timeout 5 python -c "import uvicorn; uvicorn.run('src.main:app', host='0.0.0.0', port=8000, log_level='info')" 2>&1 | head -10 || true</automated>
  </verify>
  <done>Data directories created; server startup verified; backend README created with instructions</done>
</task>

</tasks>

<verification>
Overall Phase 1 Plan 1 verification:
1. All files in files_modified list exist
2. Backend directory has complete structure
3. FastAPI server can be started with `uvicorn src.main:app --reload`
4. API documentation accessible at http://localhost:8000/docs
5. Health endpoint returns 200 OK
</verification>

<success_criteria>
- Backend project structure is complete with src/, data/, and config files
- All dependencies listed in requirements.txt
- FastAPI app starts without errors
- GET /api/health returns {"status": "healthy"}
- API documentation auto-generated and accessible
- Environment variables documented in .env.example
- User can run `pip install -r requirements.txt` successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
