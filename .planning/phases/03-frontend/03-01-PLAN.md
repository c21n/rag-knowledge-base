---
phase: 03-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/package.json",
  "frontend/tsconfig.json",
  "frontend/vite.config.ts",
  "frontend/index.html",
  "frontend/src/main.tsx",
  "frontend/src/App.tsx",
  "frontend/src/vite-env.d.ts",
  "frontend/.gitignore",
  "frontend/.env.example"
]
autonomous: true
requirements: [UI-01, UI-07]
must_haves:
  truths:
    - "React + TypeScript + Vite project is initialized"
    - "Ant Design is installed and configured"
    - "Project builds without errors"
    - "Hot reload works in development"
  artifacts:
    - path: "frontend/package.json"
      provides: "Project dependencies and scripts"
      contains: ["react", "antd", "typescript", "vite"]
    - path: "frontend/src/main.tsx"
      provides: "Application entry point"
      min_lines: 15
    - path: "frontend/src/App.tsx"
      provides: "Root component"
      min_lines: 20
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/App.tsx"
      via: "ReactDOM render"
      pattern: "createRoot.*render"
---

<objective>
Initialize React + TypeScript + Vite frontend project with Ant Design UI framework.

Purpose: Create the foundation for the frontend application with modern tooling and enterprise-grade UI components.
Output: Working frontend development environment ready for component development.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

## API Context
Backend runs on http://localhost:8000 with CORS enabled for all origins.
API documentation available at http://localhost:8000/docs
</context>

<tasks>

<task type="auto">
  <name>Initialize Vite React + TypeScript project</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/vite.config.ts, frontend/index.html</files>
  <action>
Create frontend directory and initialize project with:
1. Run `npm create vite@latest frontend -- --template react-ts` in project root
2. If npm create fails, manually create package.json with dependencies:
   - react ^18.2.0
   - react-dom ^18.2.0
   - @types/react ^18.2.0
   - @types/react-dom ^18.2.0
   - typescript ^5.2.0
   - vite ^5.0.0
   - @vitejs/plugin-react ^4.2.0
3. Create tsconfig.json with standard React + TypeScript settings
4. Create vite.config.ts with React plugin
5. Create index.html with root div and main.tsx script
  </action>
  <verify>
    <automated>cd frontend && npm install && npm run build</automated>
    <manual>Build completes without errors</manual>
    <sampling_rate>once after task</sampling_rate>
  </verify>
  <done>Vite React + TypeScript project initialized and builds successfully</done>
</task>

<task type="auto">
  <name>Install and configure Ant Design</name>
  <files>frontend/package.json, frontend/src/main.tsx, frontend/src/App.tsx</files>
  <action>
Install Ant Design and configure in project:
1. Install dependencies: `npm install antd @ant-design/icons`
2. Import Ant Design CSS in main.tsx: `import 'antd/dist/reset.css'`
3. Update App.tsx to include basic Ant Design layout components:
   - Layout with Header, Content, Footer
   - Typography.Title for app name
   - Button component to verify Ant Design works
4. Create basic app structure:
   - App header with "RAG Knowledge Base" title
   - Empty content area (will be filled later)
   - Footer with copyright
5. Configure Vite for optimal Ant Design imports (optional: install unplugin-auto-import for automatic imports)
  </action>
  <verify>
    <automated>cd frontend && npm run dev & sleep 3 && curl -s http://localhost:5173 | grep -i "rag\|knowledge" && pkill -f "vite" || true</automated>
    <manual>Dev server starts and Ant Design components render correctly</manual>
    <sampling_rate>once after task</sampling_rate>
  </verify>
  <done>Ant Design installed, configured, and basic layout renders correctly</done>
</task>

<task type="auto">
  <name>Configure development environment and tooling</name>
  <files>frontend/.env.example, frontend/.gitignore, frontend/src/vite-env.d.ts</files>
  <action>
Set up development environment configuration:
1. Create .env.example with:
   - VITE_API_BASE_URL=http://localhost:8000/api
2. Create .gitignore ignoring:
   - node_modules/
   - dist/
   - .env
   - *.log
3. Create src/vite-env.d.ts with environment type definitions:
   - Interface for ImportMetaEnv with VITE_API_BASE_URL
4. Update package.json scripts if needed:
   - "dev": "vite"
   - "build": "tsc && vite build"
   - "preview": "vite preview"
5. Create README.md in frontend/ with setup instructions
  </action>
  <verify>
    <automated>cd frontend && ls -la .env.example .gitignore src/vite-env.d.ts</automated>
    <manual>All config files exist and git ignores node_modules</manual>
    <sampling_rate>once after task</sampling_rate>
  </verify>
  <done>Development environment configured with environment variables and gitignore</done>
</task>

</tasks>

<verification>
- [ ] `npm install` completes without errors
- [ ] `npm run build` produces dist/ folder
- [ ] `npm run dev` starts development server on port 5173
- [ ] Ant Design components render correctly (Button, Layout, Typography visible)
- [ ] TypeScript compilation succeeds
- [ ] Project structure follows React + Vite conventions
</verification>

<success_criteria>
Frontend project is fully initialized with:
1. React 18 + TypeScript 5 + Vite 5 working together
2. Ant Design 5.x installed and configured
3. Development server runs on port 5173
4. Production build succeeds
5. Environment configuration ready for API integration
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend/03-01-SUMMARY.md` documenting:
- Project structure created
- Dependencies installed
- Build verification results
</output>
